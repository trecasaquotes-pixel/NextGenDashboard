# Server Configuration
PORT=5000

# CORS Configuration
# Set to your frontend URL for tighter security (e.g., https://yourdomain.com)
# If not set, the server will echo the request origin (suitable for development)
CLIENT_URL=http://localhost:5173

# Database Connection
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:port/database

# Session Secret
# Generate a strong random string for production
SESSION_SECRET=your_session_secret_here

# Render Token Secret (optional)
# Used for server-side PDF generation. Will auto-generate if not set.
RENDER_SECRET=your_render_secret_here

# Replit-specific variables (automatically provided by Replit)
# REPL_ID=
# REPL_SLUG=
# REPL_OWNER=
# REPLIT_DOMAINS=
# ISSUER_URL=
